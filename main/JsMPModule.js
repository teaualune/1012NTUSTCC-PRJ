/*global: module */

(function () {

    'use strict';

    var socketio = require('socket.io');

    module.exports = {
        test: function () {
            console.log(socketio);
        }
    };

})();
